# 修复镜像发布版本覆盖计划

## 背景

`v2.1.10` 标签在 2026-07-28 08:35（Asia/Shanghai）触发 Docker 发布并正确生成版本镜像。随后在 10:37 从 `master` 手动运行 `deploy-image.yml`，工作流读取仓库中仍为 `2.1.2` 的 `cli/VERSION`，又无条件写入 `latest` 与 `skills-full`，导致 Web 控制台左下角显示 `V2.1.2`。

远端元数据确认，`v2.1.10` 对应提交 `7aeaedd3`，被覆盖后的 `latest` 对应提交 `5ea5a9d9`。版本化镜像没有被移动，问题只影响浮动标签。

## 目标

- 正式 Docker 镜像只能由 `v*` 标签推送触发。
- 镜像内版本只能从当前发布标签派生，不再读取仓库中的旧版本文件作为发布输入。
- Web、CLI 和 Python 包元数据在 `v2.1.11` 镜像中统一显示 `2.1.11`。
- 用合同测试阻止手动发布入口和旧版本回退重新出现。
- 发布并验证 Docker Hub、GHCR 与 GitHub Release 的 `v2.1.11` 产物。

## 修改边界

- 不移动或覆盖 `v2.1.10` 及更早标签。
- 不改变 Dockerfile、镜像依赖、能力包或运行时业务逻辑。
- 不操作任何远端部署容器或用户数据。
- 仅通过推送新的 `v2.1.11` 标签触发正式发布。

## 执行步骤

- [x] 核对 UI 版本取值链路、远端镜像摘要和 GitHub Actions 运行记录。
- [x] 收紧 Docker 发布工作流为 Tag-only，并移除旧版本文件回退。
- [x] 同步仓库版本源，补充发布合同测试、协作规则和变更记录。
- [x] 编写并校验 `v2.1.11` 发行说明。
- [x] 运行定向回归、全量回归和静态检查。
- [ ] 提交并推送 `master`，创建并推送 `v2.1.11` 注释标签。
- [ ] 跟踪 GitHub Release 与两种 Docker 镜像发布到终态。
- [ ] 核对远端版本、架构、摘要与 `latest` 指向并回写结果。

## 当前状态

本地修改与验证已完成，等待提交并发布 `v2.1.11`。

## 实际改动

- 删除 `deploy-image.yml` 的 `workflow_dispatch`，正式镜像只能由 `v*` 标签推送触发。
- 发布 Job 增加 `refs/tags/v` 引用门禁，构建版本只使用 `${GITHUB_REF_NAME#v}`，不再读取 `cli/VERSION` 作为发布输入。
- 新增发布合同测试，固定 Tag-only、标签版本派生和版本同步先于 Docker 打包的约束。
- 将 `cli/VERSION` 与 `pyproject.toml` 同步为 `2.1.11`，并更新协作规则、变更记录及版本化发行说明。

## 验证结果

### 本地验证结果

- 发布版本、发行说明和 Docker 部署合同测试 18 项通过。
- 全量 Python 回归 1119 项通过，1 项按条件跳过。
- `v2.1.11` 发行说明校验、全部 GitHub Actions YAML 解析、两个版本源一致性检查和 `git diff --check` 通过。

### 远端验证结果

待 `v2.1.11` 标签触发的 GitHub Actions 完成后回写。
