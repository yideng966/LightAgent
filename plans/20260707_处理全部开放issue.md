# 处理全部开放 Issue 计划

日期：2026-07-07

## 背景

目标仓库：`yideng966/CowAgent`

当前 open issue 共 8 个：#5、#6、#7、#8、#9、#16、#21、#22。

用户要求使用多子 agent 协作模式，处理所有 open issue，并在每个 issue 解决后单独提交、推送并关闭对应 issue。

## 已完成只读审计

已启动 3 个只读子 agent 并行审计：

1. Agent Web 抓取链路：覆盖 #6、#7、#8。
2. 个人微信群链路：覆盖 #5、#9、#21、#22。
3. 消息消费循环：覆盖 #16。

审计结论：

- #6、#7、#8 属于同一 Web 检索容错链路，但应拆成 3 个原子提交。
- #16 当前 `origin/master` 未包含 issue 正文提到的 PR #1/#2 核心修复，不能直接关闭，需要补丁。
- #21 的远端分支 `origin/codex-sticker-optimization` 包含有用实现，但不能整分支合并，因为会回退当前 master 上已有的 focus 栈与多模态改动；需要按最小范围移植。
- #22 当前代码已基本复用自主进化主链路，主要缺 README 说明和最小测试确认。
- #5、#9 均存在明确代码缺口，需要最小补丁和回归测试。

## 总体执行策略

采用“一个 issue 一个提交”的串行落地方式：

1. 每个 issue 开始前读取目标文件当前内容。
2. 先补最小失败测试或静态验证。
3. 进行最小必要修改。
4. 运行对应最小测试。
5. 更新 `CHANGES.md` 与本计划文件的实际结果。
6. 提交本 issue 的全部改动。
7. 推送提交。
8. 在对应 GitHub issue 留下中文验证说明并关闭。

不直接合并 `origin/codex-sticker-optimization`；只按文件和代码块移植 #21 仍然需要的线上表情包能力。

## 执行顺序

1. #16 消息消费循环异常隔离。
2. #6 `web_fetch` 404/410 同站导航恢复。
3. #7 检索工具连续失败可恢复。
4. #8 知识库来源可信规则。
5. #5 微信群隐藏内部工具进度。
6. #9 自由回复强触发关键词。
7. #21 表情包功能优化。
8. #22 自主进化功能确认。

## 各 Issue 验收摘要

### #16 消息消费循环异常隔离

最小修改范围：`channel/chat_channel.py`、相关测试、`CHANGES.md`、本计划文件。

验收：

- 消费循环自身异常被记录，不导致线程永久退出。
- session 被并发删除或异常状态时不会拖垮后续消息。
- image-create 入队、分发、处理关键路径有必要日志。

实际改动：

- `channel/chat_channel.py`：为 `consume()` 增加 loop 级和 session 级异常隔离；缺失 session、异常 session state、pending futures、callback 注册失败均转为可记录、可继续的控制流。
- `channel/chat_channel.py`：补充 image-create 入队、分发、处理日志。
- `tests/test_robustness_fixes.py`：新增消费循环异常隔离和 image-create 日志回归测试。

验证结果：

- `python -m unittest tests.test_robustness_fixes.TestChatChannelConsumeRobustness`：新增边界测试在旧实现下失败，修复后通过。
- `python -m unittest tests.test_robustness_fixes tests.test_wechat_group_channel tests.test_models_handler`：通过，84 个测试 OK。

审查结果：

- 规格审查：APPROVED。
- 代码质量复审：APPROVED。

### #6 web_fetch 404/410 同站导航恢复

最小修改范围：`agent/tools/web_fetch/web_fetch.py`、web_fetch SSRF/恢复测试、`CHANGES.md`、本计划文件。

验收：

- HTTP 404/410 仍返回失败状态，但附带父级页面和真实同站候选链接。
- 候选链接只来自同站，且逐个通过 SSRF 校验。
- 外站、内网、非 http/https 链接被过滤。

实际改动：

- `agent/tools/web_fetch/web_fetch.py`：在网页请求遇到 404/410 时构造同站恢复提示，列出最多 2 层父级路径，并从可访问父级页面提取同站导航候选。
- `agent/tools/web_fetch/web_fetch.py`：候选链接限制为同 `scheme + netloc` 的 `http/https` URL，加入结果前继续执行 `validate_url_safe()`、`_safe_get()` 和 `raise_for_status()`，不可访问同站坏链不返回。
- `agent/tools/web_fetch/web_fetch.py`：限制最多输出 5 个候选、最多探测 5 个候选，并对重复坏链去重，避免 404 恢复退化为爬虫。
- `tests/test_security_ssrf_web_fetch.py`：新增 404/410 恢复测试，覆盖父级提示、停止猜测提示、外站/非 HTTP/内网站点过滤、不可访问候选过滤、重复坏链去重和探测次数上限。

验证结果：

- `python -m unittest tests.test_security_ssrf_web_fetch.TestWebFetchNotFoundRecovery`：新增边界测试在旧实现下失败，修复后通过，3 个测试 OK。
- `python -m unittest tests.test_security_ssrf_web_fetch`：通过，13 个测试 OK。
- `python -m py_compile agent\tools\web_fetch\web_fetch.py tests\test_security_ssrf_web_fetch.py`：通过。

### #7 检索工具连续失败可恢复

最小修改范围：`agent/protocol/agent_stream.py`、AgentStream 测试、`CHANGES.md`、本计划文件。

验收：

- `web_fetch` / `web_search` 达到连续失败保护时，不直接返回 `critical_error` 中断整个任务。
- 工具结果提示模型停止继续抓取，基于已有内容总结或向用户索要准确链接。
- 非检索关键工具保留原有 critical 保护。

实际改动：

- `agent/protocol/agent_stream.py`：新增检索工具连续失败可恢复分支，`web_fetch` / `web_search` 达到 8 次连续失败保护时返回普通 `error`。
- `agent/protocol/agent_stream.py`：可恢复提示明确要求停止继续抓取或猜测链接，优先基于已成功获取的内容总结，资料不足时向用户询问更准确链接。
- `agent/protocol/agent_stream.py`：非检索工具仍沿用原 `critical_error` 保护。
- `tests/test_agent_stream_retrieval_failure_recovery.py`：新增单元测试覆盖两个检索工具的 recoverable error 和非检索工具 critical 行为。

验证结果：

- `python -m unittest tests.test_agent_stream_retrieval_failure_recovery`：新增测试在旧实现下失败，修复后通过，3 个测试 OK。
- `python -m unittest tests.test_agent_stream_scheduler_guard tests.test_agent_stream_logging`：通过，7 个测试 OK。
- `python -m py_compile agent\protocol\agent_stream.py tests\test_agent_stream_retrieval_failure_recovery.py`：通过。

### #8 知识库来源可信规则

最小修改范围：`agent/prompt/builder.py`、`agent/prompt/workspace.py`、`skills/knowledge-wiki/SKILL.md`、静态测试、`CHANGES.md`、本计划文件。

验收：

- 知识库整理规则明确要求优先使用页面真实导航链接。
- 仅把成功读取的 URL 作为资料来源。
- 404/失败/猜测路径不能写成知识来源。

实际改动：

- `agent/prompt/builder.py`：在知识系统 prompt 中新增来源可信规则，要求优先使用页面真实暴露的导航链接，只把成功读取的 URL 写入 Source，并排除 404/410、失败请求和猜测路径。
- `agent/prompt/workspace.py`：同步更新中英文 workspace 初始化模板，确保新建工作空间也带有相同来源规则。
- `skills/knowledge-wiki/SKILL.md`：在知识库技能中补充 Web 来源可靠性要求，明确 `> Source:` 只能使用成功抓取或打开的 URL。
- `tests/test_knowledge_source_reliability_rules.py`：新增静态测试锁定系统 prompt、workspace 模板和 knowledge-wiki 技能规则。

验证结果：

- `python -m unittest tests.test_knowledge_source_reliability_rules`：新增测试在旧规则下失败，修复后通过，3 个测试 OK。
- `python -m unittest tests.test_prompt_scheduler_guidance tests.test_knowledge_web tests.test_knowledge_service`：当前 unittest 仅识别并运行 1 个测试，通过。
- `python -m py_compile agent\prompt\builder.py agent\prompt\workspace.py tests\test_knowledge_source_reliability_rules.py`：通过。
- `python -m pytest tests/test_knowledge_web.py tests/test_knowledge_service.py`：未运行，当前环境未安装 `pytest`。

### #5 微信群隐藏内部工具进度

最小修改范围：`bridge/agent_event_handler.py`、`channel/wechat_group/wechat_group_persona.py`、相关测试、`CHANGES.md`、本计划文件。

验收：

- `channel_type=wechat_group` 且 assistant 中间消息带 tool calls 时，不向微信群发送内部过程文本。
- 最终结果仍按主链路发送。
- 微信群 persona 明确约束不要汇报内部工具过程。

实际改动：

- `bridge/agent_event_handler.py`：识别 `channel_type=wechat_group`，当 `message_end` 带 `tool_calls` 且存在 assistant 中间文本时只记录日志，不调用渠道 `_send`。
- `bridge/agent_event_handler.py`：无 `tool_calls` 的最终回复和非微信群渠道保持原逻辑。
- `channel/wechat_group/wechat_group_persona.py`：三套内置 persona 均补充不要汇报查文档、检索知识库、写入记忆、检查配置、生图排队等内部工具过程的规则。
- `tests/test_agent_event_handler.py`：新增微信群抑制中间过程、非微信群保留中间消息行为的回归测试。
- `tests/test_wechat_group_persona.py`：新增内置 persona 必须包含内部过程隐藏规则的断言。

验证结果：

- `python -m unittest tests.test_agent_event_handler`：新增测试在旧实现下失败，修复后通过，2 个测试 OK。
- `python -m unittest tests.test_agent_event_handler tests.test_wechat_group_persona`：通过，11 个测试 OK。
- `python -m unittest tests.test_wechat_group_message tests.test_wechat_group_channel tests.test_wechat_group_web`：通过，116 个测试 OK。
- `python -m py_compile bridge\agent_event_handler.py channel\wechat_group\wechat_group_persona.py tests\test_agent_event_handler.py tests\test_wechat_group_persona.py`：通过。

### #9 自由回复强触发关键词

最小修改范围：`config.py`、`config-template.json`、`channel/wechat_group/wechat_group_free_reply.py`、`channel/web/web_channel.py`、`channel/web/static/js/console.js`、相关测试、`CHANGES.md`、本计划文件。

验收：

- 新增 `wechat_group_free_reply_force_keywords`。
- 命中关键词时绕过分数阈值和低信息抑制，记录 `force_keyword_match`。
- 不绕过目标群范围、blocked sender、安全/隐私/危险内容限制。
- Web 控制台可保存和展示该配置。

实际改动：

- `config.py`、`config-template.json`：新增默认配置 `wechat_group_free_reply_force_keywords: []`。
- `channel/wechat_group/wechat_group_free_reply.py`：自由回复配置返回 `force_keywords`；命中强触发关键词时记录 `force_keyword_match`，仅跳过 `low_information` 与 `below_threshold`，不清除其他抑制项。
- `channel/web/web_channel.py`：允许 Web 保存 `wechat_group_free_reply_force_keywords`，并复用字符串列表规范化。
- `channel/web/static/js/console.js`：自由回复面板新增强触发关键词输入，读取时支持逗号、中文逗号、空白和换行分隔并去重，保存时写入后端配置。
- `tests/test_wechat_group_free_reply.py`、`tests/test_wechat_group_channel.py`、`tests/test_wechat_group_web.py`：补充配置规范化、强触发绕过范围、不可绕过抑制项、通道层图片上下文缺失拦截、Web 保存和控制台暴露测试。

验证结果：

- `python -m unittest tests.test_wechat_group_free_reply`：通过，19 个测试 OK。
- `python -m unittest tests.test_wechat_group_channel.WechatGroupChannelTest.test_free_reply_image_question_is_suppressed_without_image_context`：通过，1 个测试 OK。
- `python -m unittest tests.test_wechat_group_web.WechatGroupWebTest.test_channels_save_wechat_group_free_reply_config tests.test_wechat_group_web.WechatGroupWebTest.test_console_exposes_free_reply_force_keywords_setting`：通过，2 个测试 OK。
- `python -m unittest tests.test_wechat_group_message tests.test_wechat_group_channel tests.test_wechat_group_web`：通过，117 个测试 OK。
- `python -m py_compile config.py channel\wechat_group\wechat_group_free_reply.py channel\web\web_channel.py tests\test_wechat_group_free_reply.py tests\test_wechat_group_channel.py tests\test_wechat_group_web.py`：通过。

审查结果：

- 规格审查指出强触发不可绕过项测试覆盖不足；已补充 disabled、self_message、bot_silent_notice、media_payload、生图失败讨论、冷却、小时上限、连续上限和通道层 `image_context_unavailable` 回归断言。
- 代码质量审查未发现高危跨群、安全或冷却绕过；提示 `CHANGES.md` 必须更新，已补充。

### #21 表情包功能优化

最小修改范围：表情包线上检索、发送、Web 配置、测试、README 使用路径、`CHANGES.md`、本计划文件。

验收：

- 保留现有群内本地表情包优先逻辑。
- 支持线上检索补足候选。
- 线上候选通过受控 `online_id` 发送，不向模型或群聊暴露裸 URL。
- 下载到受控缓存后复用现有本地图片发送链路。
- Web 控制台提供线上检索配置和测试预览。
- 不恢复旧 topic 体系，不回退 focus 栈和多模态当前实现。

实际改动：

- 新增 `channel/wechat_group/wechat_group_sticker_online.py`，提供线上表情包检索、关键词清洗、安全过滤、HTTPS URL 归一化、检索 endpoint allowlist、允许域名校验、GIF 开关和候选打散。
- 更新 `channel/wechat_group/wechat_group_sticker_service.py` 与 `channel/wechat_group/wechat_group_sticker_tools.py`，支持本地优先的混合检索、`online_id` 候选缓存、线上下载到受控缓存、发送冷却、每日上限和工具结果 URL 脱敏。
- 更新 `bridge/agent_bridge.py`，提示 Agent 使用 `sticker_id` 或 `online_id` 发送表情包，并禁止暴露裸 URL；文件回复保留线上表情包来源元数据。
- 更新 `config.py`、`config-template.json`、`channel/web/web_channel.py` 与 `channel/web/static/js/console.js`，补齐线上检索配置、Web 保存归一化、`/api/wechat-group/stickers/search-online` 和“群聊 -> 表情包”配置/测试入口。
- 更新 `README.md`，补充表情包检索与发送的 Web 使用路径、线上候选安全边界和运行环境确认方式。
- 更新 `tests/test_wechat_group_sticker_online.py`、`tests/test_wechat_group_sticker_service.py`、`tests/test_wechat_group_agent_bridge_tools.py` 与 `tests/test_wechat_group_web.py`，覆盖线上检索、混合检索、线上发送、URL 脱敏、配置保存和控制台入口。

验证结果：

- `python -m unittest tests.test_wechat_group_agent_bridge_tools`：新增测试先失败，修复后通过，6 个测试 OK。
- `python -m unittest tests.test_wechat_group_web.WechatGroupWebTest.test_channels_api_lists_wechat_group_humanization_defaults tests.test_wechat_group_web.WechatGroupWebTest.test_channels_save_wechat_group_humanization_config tests.test_wechat_group_web.WechatGroupWebTest.test_wechat_group_stickers_online_search_api_hides_internal_url tests.test_wechat_group_web.WechatGroupWebTest.test_console_contains_wechat_group_sticker_panel`：新增测试先失败，修复后通过，4 个测试 OK。
- `python -m unittest tests.test_wechat_group_sticker_online.WechatGroupStickerOnlineTest.test_search_online_memes_rejects_private_or_unapproved_endpoint`：新增 endpoint SSRF 回归测试先失败，修复后通过。
- `python -m unittest tests.test_wechat_group_sticker_online tests.test_wechat_group_sticker_service tests.test_wechat_group_agent_bridge_tools tests.test_wechat_group_web tests.test_wechat_group_message tests.test_wechat_group_channel`：通过，140 个测试 OK。
- `python -m py_compile config.py bridge\agent_bridge.py channel\web\web_channel.py channel\wechat_group\wechat_group_sticker_online.py channel\wechat_group\wechat_group_sticker_service.py channel\wechat_group\wechat_group_sticker_tools.py tests\test_wechat_group_sticker_online.py tests\test_wechat_group_sticker_service.py tests\test_wechat_group_agent_bridge_tools.py tests\test_wechat_group_web.py`：通过。
- `node --check channel\web\static\js\console.js`：通过。
- Playwright 打开本地 Web 控制台 `http://127.0.0.1:9899/chat`，切换到“群聊 -> 表情包”，确认线上检索开关、Provider、Endpoint、允许域名、测试线上检索和保存表情包设置入口真实渲染；验证后已停止临时 Web 进程。

审查状态：

- Volta 只读审查确认不能整分支合并 `origin/codex-sticker-optimization`，本次只摘取表情包线上检索、受控发送、Web 配置和测试入口，不回退 focus 栈或多模态链路。
- Euclid 只读审查指出线上检索 endpoint 存在 SSRF 风险；已补充 endpoint allowlist、内网/localhost 拦截复用和回归测试。

### #22 自主进化功能确认

最小修改范围：`README.md`、相关测试、`CHANGES.md`、本计划文件。

验收：

- README 写清自主进化功能、触发链路和配置项。
- README 写清微信群复用 `bridge/agent_bridge.py` 的自主进化记录逻辑。
- README 写清微信群记录用户轮次但默认不设置主动推送 receiver，避免主动打扰群。
- 测试确认微信群上下文记录自主进化轮次时不设置主动推送目标。

实际改动：

- 更新 `README.md`，补充 `self_evolution_enabled` 总开关、`self_evolution_idle_minutes` / `self_evolution_min_turns` 的运行时 fallback 默认值，写清自主进化只作用于 Agent 模式主链路，以及 `AgentBridge.agent_reply`、`agent.chat.service`、`agent.evolution.trigger` 与 `agent.evolution.executor.run_evolution_for_session()` 的触发和执行链路。
- 更新 `README.md`，补充自主进化 review agent 的受限工具、工作空间写入边界、备份、`memory/evolution/YYYY-MM-DD.md` 记录、`remember_scheduled_output` 注入 `[EVOLUTION]` 和 `evolution_undo` 回滚说明。
- 更新 `README.md`，确认 `WechatGroupChannel -> ChatChannel -> Bridge.fetch_agent_reply -> AgentBridge.agent_reply` 复用自主进化；`wechat_group` 群聊只记录轮次，不设置主动推送 receiver。
- 新增 `tests/test_self_evolution_docs.py`，静态锁定 README 链路说明、微信群复用说明，并用 AST 检查代码中群聊 `receiver=""` 和只有 `channel_type and receiver` 才主动通知的约束。
- 更新 `tests/test_evolution.py`，给测试脚本中的 UTF-8 临时文件读取补齐显式编码，避免 Windows 默认 GBK 环境下直接运行自主进化验证脚本失败。

验证结果：

- `python -m unittest tests.test_self_evolution_docs`：新增测试先失败，补充 README 后通过，3 个测试 OK。
- `python tests\test_evolution.py`：通过，13 个 stub 场景通过，undo 校验通过。
- `python -m py_compile bridge\agent_bridge.py agent\evolution\trigger.py agent\evolution\executor.py agent\chat\service.py tests\test_self_evolution_docs.py tests\test_evolution.py`：通过。

## Git 与发布约束

- 主 checkout 存在用户既有未跟踪计划文件：`plans/20260707_群画像识别判定.md`。本任务不修改、不提交该文件。
- 在 `.worktrees/open-issues-20260707` 隔离工作区执行。
- 每个 issue 独立提交，commit message 使用简体中文。
- 每个提交推送到 `origin/master` 后再关闭对应 issue。
- 关闭 issue 时用中文说明改动文件和验证命令。

## 当前状态

- [x] 获取 open issue 列表。
- [x] 读取 issue 正文和评论。
- [x] 启动多子 agent 并行只读审计。
- [x] 汇总 issue 分组、风险和执行顺序。
- [x] 用户确认执行计划与隔离 worktree 创建。
- [x] #16 修复、提交、推送、关闭。
- [x] #6 修复、提交、推送、关闭。
- [x] #7 修复、提交、推送、关闭。
- [x] #8 修复、提交、推送、关闭。
- [x] #5 修复、提交、推送、关闭。
- [x] #9 修复、提交、推送、关闭。
- [x] #21 修复、提交、推送、关闭。
- [x] #22 修复、提交、推送、关闭。
