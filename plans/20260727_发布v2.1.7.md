# 发布 v2.1.7 计划

## 目标

- 汇总 `v2.1.6..origin/master` 的用户可感知变更，生成 `docs/releases/v2.1.7.md`。
- 按仓库发布规范校验发行说明，创建发布提交与 `v2.1.7` 注释标签。
- 将 `master` 与 `v2.1.7` 推送到 `origin`，并核对远端引用。

## 变更范围

- 发布说明只记录已合入主分支的能力与修复，不写内部计划、提交列表或未经验证的结论。
- 不修改普通开发版本文件；标签工作流会在构建时通过 `scripts/stamp_release_version.py` 同步 Python 版本元数据。
- 保留工作区既有的 `plans/20260727_内置更新与重启功能开发计划.md` 删除状态，不纳入本次提交。

## 执行步骤

- [x] 核对工作区、远端、最近标签与发布规范。
- [x] 将本地 `master` 快进到 `origin/master`，确认待发布提交范围。
- [x] 编写并校验 `v2.1.7` 发行说明。
- [x] 运行发布相关回归与最小静态检查。
- [x] 提交发布文件并创建 `v2.1.7` 标签。
- [x] 推送主分支与标签，核对远端结果。

## 当前状态

已完成。发布版本为 `v2.1.7`，范围包括全局/微信群记忆隔离、群梦境蒸馏、知识库按稳定群隔离，以及发布流程与构建版本元数据修复。

## 实际改动

- 新增 `docs/releases/v2.1.7.md`，按用户价值汇总新增功能、优化改进、Bug 修复、安装方式和文档入口。
- 新增本计划文档，记录版本选择、发布边界、验证结果和远端交付状态。
- 未修改普通开发版本文件；发布工作流会从 `v2.1.7` 标签提取版本并在构建时同步元数据。
- 未纳入工作区既有的 `plans/20260727_内置更新与重启功能开发计划.md` 删除状态。

## 验证结果

- `python scripts/validate_release_notes.py --tag v2.1.7 docs/releases/v2.1.7.md` 通过。
- 发布说明、版本同步和 Docker 部署相关回归 14 项通过。
- 全量 Python 回归 1109 项通过，1 项按条件跳过。
- `node --check channel/web/static/js/console.js`、配置模板 JSON 解析和 `git diff --check` 通过。
- 远端 `master` 与 `refs/tags/v2.1.7` 已核对指向本次发布提交。

## 剩余事项

无。标签推送后由 GitHub Actions 构建镜像和桌面资产，并创建或更新对应 GitHub Release。
