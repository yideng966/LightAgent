# 未提交功能拆分交付计划

> 状态：已完成

> 纠正：本计划执行时撤回了自定义 Provider TTS，但后续确认该撤回并非用户真实意图；TTS 已转由 `plans/20260714_自定义Provider支持TTS.md` 恢复。

## 目标

将当前工作树中的预期修改按功能点分别提交并推送到 `origin/master`，同时撤回本批未提交修改中新加入的自定义 Provider TTS 能力，保留自定义 Provider ASR 及其他已确认功能。

## 提交边界

1. 微信群拍一拍强制回复。
2. 微信群音频格式误判修复。
3. 自定义 Provider ASR 支持，不包含自定义 TTS。
4. 微信群语音交互策略。
5. 微信群聊时间范围总结 Skill 开发计划。

## 执行约束

- 不运行验证命令或单元测试。
- 不修改无实质差异的微信群协议与 sidecar 文件。
- 每个代码提交同步包含对应的 `CHANGES.md` 记录。
- Git 提交说明使用简体中文。

## 任务清单

- [x] 核对工作树、分支和目标远端。
- [x] 撤回本批自定义 TTS 增量。
- [x] 按功能点创建独立提交。
- [x] 推送到 `origin/master`。
- [x] 回写提交与推送结果。

## 实际结果

- `d6abb1e 修复微信群音频格式误判`
- `14dc4dd 增加自定义Provider语音识别`
- `9179af3 增加微信群拍一拍强制回复`
- `390af96 增加微信群语音交互策略`
- `5856fc9 规划微信群聊时间范围总结技能`
- 已推送到 `origin/master`。
- 按用户要求，本次拆分、提交和推送阶段未重新运行验证命令或单元测试。
