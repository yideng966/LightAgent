# 微信群表情包回复频率优化计划

## 目标

- 普通 @、引用回复和自由回复在轻松语境下都能按可控概率主动使用表情包。
- 新收藏的微信表情包不再把传输层 XML 保存为描述。
- 保留现有冷却、日上限、群隔离和先搜索后发送约束。

## 实施步骤

- [已完成] 梳理表情包描述、回复策略和配置保存链路。
- [已完成] 增加最小频率配置并接入回复策略。
- [已完成] 修复新收藏表情包描述生成。
- [已完成] 补充并运行相关回归测试。
- [已完成] 回写实际改动、验证结果与剩余事项。

## 风险控制

- 不修改用户当前 `config.json` 配置值。
- 不绕过表情包冷却、每日上限和当前群作用域。
- 不对既有素材库做批量迁移，避免无可靠语义时错误改写历史描述。

## 验证计划

- 运行表情包服务、工具、回复策略与 Web 配置相关测试。
- 运行 Python 语法检查。
- 重启 CowAgent 并确认 Web 端口及微信群 sidecar 恢复。

## 实际改动

- 新增 `wechat_group_sticker_reply_percent`，默认值 20，Web 控制台允许在 0–100 之间调整。
- 自由回复、普通 @、引用等回复策略统一获得表情包工具使用指引和目标频率。
- 新收藏表情包的 `<msg><emoji>` XML 描述会回退为安全文件名，不再进入素材描述。
- 未修改用户当前 `config.json`，未迁移既有历史表情包记录。

## 验证结果

- 表情包服务、Agent 工具、回复策略和 Web 配置定向回归通过，22 项测试 OK。
- 完整 Web 测试通过，82 项测试 OK；完整微信群上下文测试通过，23 项测试 OK。
- Python 语法检查、控制台 JavaScript 语法检查和差异检查通过。

## 剩余事项

- 完整 `tests.test_wechat_group_channel` 含长等待用例，组合运行两次分别在 3 分钟和 5 分钟工具超时前未报告失败；本次相关路径已由定向测试覆盖。
- 历史 XML 描述保持不变；工具层仍会将其脱敏显示为通用 `sticker`。

## 部署状态

- CowAgent 已重启，新进程 PID 为 21816。
- Web 控制台已恢复监听 9901 端口，微信群 sidecar 已启动。
- `config.json` 未显式写入新字段，当前使用默认主动回复频率 20%。

## 历史描述清洗

- [已完成] 只读统计历史素材描述污染范围并确认迁移规则。
- [已完成] 使用 Vision 生成真实语义标签，并备份、清洗历史 XML 描述。
- 统计结果：329 条素材中有 143 条 `<msg><emoji>` XML 描述；对应文件均存在，但文件名为无语义纯数字消息 ID。
- 清洗规则：逐张生成“主体 + 动作或表情 + 情绪或意图”的中文短标签；只更新 Vision 成功项，失败项保留 XML 以便续跑，其余 186 条描述保持不变。
- 实际结果：134 条已更新为通过质量门禁的 Vision 语义；8 条按用户要求保留 XML 并停止处理；1 条 0 字节媒体标记为“媒体文件为空，无法识别内容”并禁用。
- 完整回退备份：`wechat_group_sticker.db.semantic-labels.20260717_174849.bak`。
- Web 表情包卡片现有“描述”字段会直接展示清洗后的语义；当前没有独立多标签字段。
- 新表情包自动收藏当前不调用 LM/Vision，描述来自媒体文件名；纯数字或哈希文件名会回退为“群聊表情包”。
- 用户要求停止处理剩余异常项后，Vision 批处理已停止；最终保留 8 条 XML 描述不再更新。
- CowAgent 已再次重启，新进程 PID 为 11560，Web 端口 9901 与微信群 sidecar 正常。

## 历史数字描述补充修复

- [已完成] 修复新入库时传入描述本身为纯数字/长哈希的漏判。
- [已完成] 仅对历史 `opaque` 描述执行 Vision 语义标注，不处理保留的 8 条 XML。
- 历史 190 条纯数字描述已完成标注和有限续跑，最终 `opaque` dry-run 候选为 0。
- 迁移脚本支持 1–4 个并发 Vision 请求，SQLite 更新保持在主线程逐条执行。
