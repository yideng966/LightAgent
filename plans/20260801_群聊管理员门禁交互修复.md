# 群聊管理员门禁交互修复计划

## 任务背景

修复 Web 控制台“群聊 -> 群与管理员”中的管理员门禁详情展开后滚动到顶部问题，并让微信群普通成员触发管理员门禁时保持静默，不再向群内发送权限提示文本。

## 实施步骤

- [x] 检查工作区状态，定位管理员门禁 UI、通道硬门禁与发送链路。
- [x] 复用群聊页面既有滚动保留机制修复详情展开交互。
- [x] 保留通道硬门禁，在发送层静默管理员权限拒绝回复。
- [x] 补充 UI 与微信群通道回归测试，并同步相关文档和 `CHANGES.md`。
- [x] 运行聚焦测试、JavaScript 语法检查和 Git 差异检查。
- [x] 检查提交范围并使用简体中文提交说明提交 Git。

## 约束

- 不改变管理员权限判定、配置结构或 Agent 工具过滤逻辑。
- 仅静默管理员权限拒绝；身份未确认、功能未启用等其他业务错误继续反馈。
- 不修改或提交工作区中与本任务无关的文件。

## 当前状态

已完成。

## 实际改动

- 管理员门禁详情切换改为调用 `renderGroupsView({ preserveScroll: true })`，沿用现有滚动位置恢复机制。
- 通道硬门禁命中管理员权限时写入 `wechat_group_silent_admin_guard`，微信群发送层据此跳过权限提示；其他错误仍按原逻辑发送。
- 管理员限定的群聊报告触发同样使用静默标记。
- 同步中英文微信群文档、项目协作规则和变更记录。

## 验证结果

- `python -X utf8 -m unittest tests.test_wechat_group_channel`：151 项通过；新增报告门禁测试后，4 项门禁聚焦用例通过。
- `python -X utf8 -m unittest tests.test_wechat_group_web`：119 项通过；管理员门禁 UI 聚焦用例通过。
- `node --check channel/web/static/js/console.js`：通过。
- `git diff --check`：通过。

## 剩余事项

无。
