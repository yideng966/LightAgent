# 回退图片摘要改动并发布 LightAgent 2.2.10

## 目标

使用可审计的反向提交撤销 `0e0d97991fa51b711fab5a06edcb3fbb00f55641` 引入的代码、测试与变更记录，在不改写既有发布历史的前提下发布 `v2.2.10`，并同步更新 GitHub Release。

## 版本范围

- 基准标签：`v2.2.9`
- 目标标签：`v2.2.10`
- 主要变化：回退图片理解无效摘要判定与自由回复静默条件的调整，恢复 `0e0d9799` 之前的处理逻辑。

## 实施步骤

- [x] 核对当前分支、远端、工作区、目标提交和现有标签。
- [x] 使用 `git revert --no-commit 0e0d9799...` 生成最小反向改动并检查差异。
- [x] 使用 `scripts/stamp_release_version.py` 同步 `cli/VERSION` 与 `pyproject.toml` 为 `2.2.10`。
- [x] 新增 `docs/releases/v2.2.10.md`，更新 `CHANGES.md`，并检查 `AGENTS.md` 状态。
- [x] 运行回退相关定向测试、发行合同测试、发行说明校验和差异检查。
- [ ] 创建简体中文发行提交和附注标签 `v2.2.10`。
- [ ] 将 `master` 与 `v2.2.10` 推送到 `origin`。
- [ ] 创建或更新 GitHub Release，并核对标题、正文和发布状态。
- [ ] 回写本计划的实际改动、验证结果与剩余事项。

## 风险与边界

- 只撤销指定提交，不回退其后的 Sidecar 安装改进、发行说明规则或其他提交。
- 回退会恢复指定提交之前的图片摘要判定和自由回复行为；不额外设计替代修复。
- 保留工作区现有的 5 个无关未跟踪文件，不纳入提交或标签。
- 不连接、更新或重启远端部署环境；标签和 Release 成功后不等待 GitHub Actions 或镜像构建终态。
- 推送前若远端 `master` 或 `v2.2.10` 状态发生变化，停止发布并重新核对，不覆盖远端并发更新。

## 验收标准

- `git show v2.2.10` 包含指定提交的反向改动、版本文件、发行说明、`CHANGES.md` 和本计划。
- `cli/VERSION` 与 `pyproject.toml` 均为 `2.2.10`。
- `docs/releases/v2.2.10.md` 通过版本化发行说明校验。
- `origin/master` 与远端 `v2.2.10` 推送成功，GitHub Release 为已发布状态且正文与仓库发行说明一致。

## 实施结果

待执行完成后回写。

## 剩余事项

待执行完成后回写。
