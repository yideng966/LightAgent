# README 中文重写计划

## 背景

根目录 `README.md` 当前以英文为主，且存在中文链接与图标文案乱码。用户要求基于 `CHANGES.md`、`AGENTS.md` 和实际代码重写 README，使用中文描述，在架构描述基础上完整列举功能与内部逻辑，并保留启动命令。

## 范围

- 修改 `README.md`：重写为简体中文主文档。
- 不修改业务代码、配置、测试和现有用户未提交改动。
- 纯文档变更，不更新 `CHANGES.md`。

## 信息来源

- `README.md`：保留项目定位、安装启动命令、CLI 命令、免责声明等公开说明。
- `CHANGES.md`：提取 2026-07-01 至 2026-07-05 的微信群、Web UI、Agent 日志、迁移、桌面端等近期能力。
- `AGENTS.md`：提取项目边界、目录职责、核心数据流、验证策略和微信群通道约束。
- 实际代码：
  - `app.py`
  - `config.py`
  - `common/const.py`
  - `channel/channel_factory.py`
  - `bridge/bridge.py`
  - `bridge/agent_bridge.py`
  - `agent/tools/__init__.py`
  - `agent/tools/tool_manager.py`
  - `models/bot_factory.py`
  - `desktop/package.json`

## 执行步骤

- [x] 读取现有 README、CHANGES、AGENTS 和关键代码入口。
- [x] 确认当前工作区已有用户改动，避免触碰无关文件。
- [x] 重写 `README.md`，覆盖项目定位、架构、功能、内部逻辑、目录职责、运行命令、配置、验证、开发约束和免责声明。
- [x] 静态检查 README 是否为中文、关键命令是否保留、核心代码链路是否和实际代码一致。
- [x] 回写本计划的实际改动、验证结果和剩余事项。

## 验证方式

- 使用 PowerShell 读取 `README.md`，确认没有旧乱码文案。
- 使用文本检索确认保留以下命令：
  - `python app.py`
  - `cow start`
  - `python -m unittest discover -s tests`
  - `npm run build`
  - `npm run dev`
- 使用 `git diff -- README.md plans/20260705_README中文重写.md` 检查改动范围。

## 实际改动

- 重写 `README.md` 为简体中文主文档。
- 保留原有徽章、Logo、安装命令、源码启动命令、CLI 命令、桌面端命令、测试命令和免责声明。
- 新增并细化以下说明：
  - 项目定位与核心能力。
  - `app.py`、`ChannelManager`、`channel_factory`、`Bridge`、`AgentBridge`、`AgentStreamExecutor` 的内部流转。
  - 渠道、模型、Web 控制台、桌面端、Agent、工具、MCP、技能、记忆、知识库、插件、定时任务、多模态和个人微信群通道能力。
  - 个人微信群通道的 Python / sidecar 边界、上下文注入顺序、已实现能力和安全边界。
  - 安全风险区域、测试命令和常见开发路径。

## 验证结果

- `Get-Content -LiteralPath 'D:\JiangShuai\SourceCode\CowAgent\README.md' -Encoding UTF8 -TotalCount 80`
  - 已确认 README 开头为正常中文，无旧版链接乱码。
- `Select-String -Path 'D:\JiangShuai\SourceCode\CowAgent\README.md' -Pattern '馃|鈥|涓|鏃|�|pyttsx|&&'`
  - 无匹配结果，未发现旧乱码特征、错误 Provider 名或 Windows 禁用命令串联。
- `Select-String -Path 'D:\JiangShuai\SourceCode\CowAgent\README.md' -Pattern 'python app.py|cow start|python -m unittest discover -s tests|npm run build|npm run dev|node --check|python -m pip install -r requirements.txt'`
  - 已确认关键启动、CLI、测试、前端检查和桌面端命令均保留。
- PowerShell 统计 Markdown 代码围栏数量：
  - `code_fences=48`，数量为偶数。
- PowerShell 统计 README 规模：
  - `chars=16145`
  - `headings=24`

## 剩余事项

- 本次为纯文档重写，未运行 Python 单元测试或桌面端构建。
- 未更新 `CHANGES.md`，原因是项目规则明确纯文档、计划、规则、配置说明等非代码变更不更新 `CHANGES.md`。
