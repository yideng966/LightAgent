# 回退并发布 v2.2.0 计划

## 状态

已完成

## 目标

将 `master` 的代码基线回退到 `172d6f69dbe7e064813e4b028c7a2ca628347126`，只重放以下两个提交的变更：

- `129011ff36df5246018b9db0225d78fd9cb48dbe`：中文化贡献指南并规范提交语言
- `a2a214791b72f6f03a3b75c27fdaad3ec56978ba`：修复 Docker 清单发布竞态并发布 2.1.21

在此代码树上发布 `v2.2.0`，提交并推送 `master` 与附注标签。

## 执行步骤

- [x] 核对当前分支、远端、工作区和目标提交拓扑。
- [x] 在临时 worktree 中演练两个提交的重放并记录冲突处理方式。
- [x] 保留当前远端指针，重置 `master` 到目标基线。
- [x] 依次重放两个指定提交，解决仅由被排除中间提交造成的冲突。
- [x] 更新版本来源、`CHANGES.md`、发行说明和本计划的实际结果。
- [x] 运行发行说明、版本、相关回归和差异检查。
- [x] 创建并推送 `v2.2.0`，按远端保护规则推送重建后的 `master`。

## 验收标准

- 重建后的提交历史从 `172d6f69` 开始，之后仅包含两个指定提交的重放及本次发布元数据提交。
- `cli/VERSION` 与 `pyproject.toml` 均为 `2.2.0`。
- `docs/releases/v2.2.0.md` 通过发布说明校验并已进入打标签提交。
- `master` 和附注标签 `v2.2.0` 成功推送到 `origin`。

## 回退边界

- 推送前发现问题时只在本地修正，不移动远端标签。
- 本地保留重建前的远端提交引用，便于在推送前恢复检查。
- 不处理工作区现有的未跟踪文件，也不连接或操作远端运行环境。

## 实际改动

- `129011ff` 已重放为 `3c5485e5`，只修改 `CONTRIBUTING.md`。
- `a2a21479` 已重放为 `aaa3e75b`；冲突仅出现在 `CHANGES.md`、`cli/VERSION` 与 `pyproject.toml`，处理时未带入被排除的 2.1.19/2.1.20 提交内容。
- 版本来源已同步为 `2.2.0`，并新增 `docs/releases/v2.2.0.md` 说明回退范围和升级注意事项。

## 验证结果

- `python scripts/validate_release_notes.py --tag v2.2.0 docs/releases/v2.2.0.md`：通过。
- 发布说明、版本与 Docker 发布契约定向测试：19 项通过。
- `python -X utf8 -m unittest discover -s tests`：共运行 1211 项，结果 OK，1 项按条件跳过。
- 两个版本来源一致，目标历史与文件范围符合计划，`git diff --check` 通过。

## 剩余事项

- 无。本任务不等待 GitHub Actions 或镜像构建终态。

## 发布结果

- 重建后的 `master` 使用 `--force-with-lease` 保护远端并发更新，与附注标签 `v2.2.0` 一并原子推送到 `origin`。
- `v2.2.0` 指向包含版本文件、发行说明、变更记录与本计划的发布提交。
- 未连接、更新或重启远端运行环境，未等待标签触发的 GitHub Actions 或镜像构建终态。
