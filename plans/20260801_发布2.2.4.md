# 发布 LightAgent 2.2.4

## 目标

发布 `v2.2.4` 补丁版本，将 GitHub Release 群通知详情增强交付给用户，并保证标签内版本来源、发行说明和变更记录一致。

## 版本范围

- 基准标签：`v2.2.3`
- 目标标签：`v2.2.4`
- 主要变化：GitHub Release Webhook 群通知展示版本摘要与变更详情，并过滤安装、文档等尾部内容。

## 实施步骤

- [x] 核对 `v2.2.3..HEAD` 的提交与文件差异。
- [x] 新增 `docs/releases/v2.2.4.md` 中文发行说明。
- [x] 使用发布脚本同步 `cli/VERSION` 与 `pyproject.toml`。
- [x] 更新 `CHANGES.md` 并完成发行校验与相关回归测试。
- [x] 创建发行提交和 `v2.2.4` 标签并推送远端。

## 风险与边界

- 本版本不修改配置、数据库或渠道协议，不包含破坏性迁移。
- 发行说明只描述 `v2.2.3..HEAD` 已确认的用户可感知变化。
- 标签推送成功即完成，不等待 GitHub Actions 或镜像构建。

## 实施结果

- 已新增面向用户的 `docs/releases/v2.2.4.md`，内容仅覆盖 `v2.2.3..HEAD` 的 GitHub Release 群通知增强。
- 已使用 `scripts/stamp_release_version.py` 将 `cli/VERSION` 与 `pyproject.toml` 同步为 `2.2.4`。
- 发行说明校验通过；发行说明、版本和 Docker 发布契约测试 19 项通过。
- GitHub Webhook 与 scheduler 投递回归 43 项通过，`git diff --check` 通过。
- 已创建简体中文发行提交和附注标签 `v2.2.4`，并将 `master` 与标签推送到 `origin`。

## 剩余事项

无。按仓库规则不等待 GitHub Actions 或镜像构建终态。
