# 仓库私有化与重命名计划

## 目标

- 将当前公开 fork 迁移为独立私有仓库 `yideng966/wechatAgent`。
- 新仓库仅保留 `master` 分支，不保留其他远端分支和标签。
- 本地 `origin` 切换到新仓库，并移除 `upstream`。
- 删除非 `master` 的本地分支及其 linked worktree。
- 新仓库验证通过后，删除旧公开仓库 `yideng966/CowAgent`。

## 迁移前状态

- 当前 `master` 与 `origin/master` 指向同一提交 `7f31d4a`。
- 5 个附加 worktree 均无未提交修改，可以安全移除。
- 主工作区存在用户未提交文件，本次迁移不得修改、暂存或清理这些文件。
- 目标仓库 `yideng966/wechatAgent` 当前不存在。

## 实施步骤

- [x] 在仓库外创建包含全部现有 Git 引用的 bundle，并完成校验。
- [x] 创建私有、独立的 `yideng966/wechatAgent`。
- [x] 仅推送本地 `master`，并设为默认分支。
- [x] 验证新仓库为 private、非 fork、只有 `master`，且提交与本地一致。
- [x] 将本地 `origin` 切换到新仓库并移除 `upstream`。
- [x] 移除附加 worktree 和所有非 `master` 本地分支。
- [x] 删除旧公开仓库 `yideng966/CowAgent`。
- [x] 复核 GitHub 可见性、远程地址、分支和主工作区未提交状态。

## 边界与回退

- 本次仅修改 GitHub 仓库名称与 Git 远程关系，不批量替换源码中的 `CowAgent` 品牌名称。
- 本次不移动主工作区目录，避免当前会话和本机工具路径失效。
- 删除旧仓库和本地分支前，必须确保 bundle 校验成功且新私有仓库验证通过。
- 如需恢复已删除分支，可从仓库外的 bundle 重新创建引用。

## 执行结果

- 状态：已完成。
- 实际改动：创建私有独立仓库 `yideng966/wechatAgent`，仅迁移 `master`；本地 `origin` 已切换到新仓库，`upstream`、临时迁移远程、5 个附加 worktree 和 7 个非 `master` 本地分支已移除；旧公开仓库 `yideng966/CowAgent` 已按最终确认保持删除状态。
- 验证结果：新仓库 `private = true`、`fork = false`、默认分支及唯一分支均为 `master`；本地与远端 `master` 均指向 `7f31d4a`；主工作区原有未提交内容保持未暂存状态。
- 回退资料：`D:\JiangShuai\SourceCode\CowAgent-before-wechatAgent-20260722.bundle` 已通过 `git bundle verify`。
- 剩余事项：本次未移动本地工作区目录，也未批量修改源码中的 `CowAgent` 品牌名称。
